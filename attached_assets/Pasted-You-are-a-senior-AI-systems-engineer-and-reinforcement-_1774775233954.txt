You are a senior AI systems engineer and reinforcement learning environment designer.

Generate a complete production-ready repository implementing an OpenEnv-compatible environment called:

AI Workspace Assistant Environment

The environment simulates a digital workspace where an AI agent completes productivity tasks such as email search, meeting scheduling, and document organization.

The system must be compatible with OpenEnv validators and HuggingFace Spaces deployment.

---

## PRIMARY GOAL

Build an environment that allows an LLM agent to interact using the standard RL interface:

reset()
step(action)
state()

The environment must return:

observation
reward
done
info

---

## PROJECT STRUCTURE

Generate the following repository:

openenv-workspace-env/

├── openenv.yaml
├── Dockerfile
├── requirements.txt
├── inference.py
├── README.md
├── server.py
│
├── env/
│   ├── environment.py
│   ├── actions.py
│   ├── state.py
│   ├── tasks.py
│   └── graders.py
│
├── replay/
│   ├── replay_logger.py
│
├── dashboard/
│   ├── dashboard.html
│   ├── dashboard.js
│
├── utils/
│   ├── reward_engine.py
│   ├── helpers.py

---

## ENVIRONMENT SIMULATION

Simulate a digital workspace containing:

• Email Inbox
• Calendar
• Document Folders
• Task Manager

The environment state must include:

{
current_app,
email_list,
selected_email,
calendar_events,
documents,
task_progress
}

---

## SUPPORTED ACTIONS

open_email_inbox()
search_email(sender)
read_email(email_id)
extract_meeting_details()
create_calendar_event()
move_document(folder)
view_calendar()
view_documents()
noop()

Actions must update environment state logically.

---

## TASK DESIGN (3+ TASKS REQUIRED)

Task 1 — Email Retrieval

Goal:
Find the email from "Alex".

---

Task 2 — Meeting Scheduling

Goal:
Read email and create a calendar event.

---

Task 3 — Document Organization

Goal:
Move document to the correct folder.

---

## REWARD SYSTEM

Implement a reward engine:

correct navigation → +0.2
correct action → +0.3
task completion → +1.0
incorrect action → −0.1
invalid navigation → −0.2

Rewards must stay within range 0.0–1.0.

---

## GRADERS

Implement automated graders for each task.

Return:

0.0 → failure
0.5 → partial
1.0 → success

---

## OPENENV CONFIGURATION

Create openenv.yaml specifying:

environment name
version
tasks
grader mappings
API endpoints

---

## FASTAPI SERVER

Implement API endpoints:

POST /reset
POST /step
GET /state
GET /episode_replay

The server should return JSON responses.

---

## EPISODE REPLAY SYSTEM

Implement a replay logger that records every step.

Store:

step_number
action
state
reward

Example log:

[
{
"step": 1,
"action": "open_email_inbox()",
"reward": 0.2
}
]

Expose replay via:

GET /episode_replay

---

## VISUALIZATION DASHBOARD

Serve a dashboard at:

GET /

The dashboard must display:

• current environment state
• agent action timeline
• reward progression graph
• task completion status

Use simple HTML + JavaScript.

Use Chart.js for reward visualization.

---

## INFERENCE SCRIPT

Create inference.py.

Requirements:

Use OpenAI Python client.

Read environment variables:

API_BASE_URL
MODEL_NAME
HF_TOKEN

Agent loop:

1. reset environment
2. receive observation
3. send observation to LLM
4. receive action
5. execute step(action)
6. log reward
7. stop when done or max steps reached

Ensure runtime < 20 minutes.

---

## DOCKER SUPPORT

Provide Dockerfile:

• base image python:3.10
• install requirements
• run FastAPI server
• expose port 7860

Docker build must succeed.

---

## HUGGINGFACE DEPLOYMENT

Ensure project deploys on HuggingFace Spaces.

Validator must receive HTTP 200 from:

/reset

---

## SYSTEM CONSTRAINTS

Must run on:

2 vCPU
8GB RAM
runtime < 20 minutes

---

## CODE QUALITY

Ensure:

• modular architecture
• type hints
• docstrings
• deterministic environment
• lightweight dependencies
• structured logging

---

## README REQUIREMENTS

README must include:

• project overview
• environment description
• task explanation
• reward system
• deployment instructions
• API documentation
• how to run inference.py
• how to deploy to HuggingFace Spaces

---

## FINAL OUTPUT

Generate complete working code for every file in the repository.
